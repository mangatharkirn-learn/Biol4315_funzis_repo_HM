---
title: "Biol4315 Lab-1 Report"
output: html_document
date: "2026-09-10"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
# AnnaMehrag

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

# Code to Install vegan Package
```{r, eval=FALSE}
install.packages("vegan")
```


## Scatter plot of speed vs dist
The [ggplot2](https://ggplot2.tidyverse.org) package is used to visualize a scatter plot of speed vs dist from the cars dataset.

```{r, include=FALSE}
install.packages("ggplot2", repos = "https://cloud.r-project.org")
```

```{r, echo=FALSE}
library(ggplot2)
ggplot(cars,aes(speed,dist)) + geom_point()
```

```{bash eval=FALSE}
docker pull nextgenusfs/funannotate
```

```{bash. eval=FALSE}
conda create -n sequali-env python=3.11 -c conda-forge
conda activate sequali-env
conda install -c conda-forge -c bioconda sequali
```

